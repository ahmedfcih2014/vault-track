{
  "name": "@vault-track/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "test": "vitest run --config vitest.config.ts",
    "test:watch": "vitest --config vitest.config.ts",
    "test:smoke": "npm run build && node scripts/smoke-test.cjs"
  },
  "dependencies": {
    "decimal.js": "^10.6.0",
    "luxon": "^3.7.1",
    "zod": "^4.0.17"
  },
  "devDependencies": {
    "@types/luxon": "^3.7.1",
    "typescript": "~6.0.2",
    "vitest": "^3.2.4"
  }
}
